{
  "name": "gatsby-starter-default",
  "private": true,
  "description": "React credit cardstest with gatsby",
  "version": "0.1.0",
  "dependencies": {
    "gatsby": "^2.22.15",
    "react": "^16.12.0",
    "react-credit-cards": "^0.8.2",
    "react-dom": "^16.12.0"
  },
  "devDependencies": {
    "prettier": "2.0.5"
  },
  "keywords": [
    "gatsby"
  ],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write \"**/*.{js,jsx,json,md}\"",
    "start": "npm run develop",
    "serve": "gatsby serve",
    "clean": "gatsby clean",
    "test": "echo \"Write tests! -> https://gatsby.dev/unit-testing\" && exit 1"
  }
}
